{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.color) }}
    <button type="submit" class="btn btn-primary">
        <i class="bi bi-check-lg me-1"></i>{{ button_label|default('Save') }}
    </button>
    <a href="{{ path('admin_label_index') }}" class="btn btn-secondary ms-2">Cancel</a>
{{ form_end(form) }}
