{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.position) }}
    <button type="submit" class="btn btn-primary">
        <i class="bi bi-check-lg me-1"></i>{{ button_label|default('common.save'|trans) }}
    </button>
    <a href="{{ path('admin_column_index') }}" class="btn btn-secondary ms-2">{{ 'common.cancel'|trans }}</a>
{{ form_end(form) }}
